export interface ResultButtonInterface {
  text: string;
  url?: string;
  urls?: any;
}

export interface AnimalInterface {
  // 動物名稱
  text: string;
  // 動物圖片
  url: string;
  // 分享用的動物結果連結
  shareUrl: string;
}
